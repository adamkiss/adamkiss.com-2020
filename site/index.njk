---
title: •
repository: adamkiss/adamkiss.com
---

{% extends "layout.njk" %}

{% block content %}
  <div class="py-24">
  
    ✍ Read
    🐦 Tweet
    💌 Email

    <h2 class="text-5xl font-serif">Now</h2>
    <p class="text-2xl leading-9">Lorem ipsum dolor, sit amet consectetur adipisicing elit. Magnam a cupiditate sint dolor aperiam, omnis temporibus sapiente inventore ex voluptatum, quasi vitae impedit dolores rerum pariatur numquam sequi adipisci ipsam.</p>

    <h2 class="mt-12 text-5xl font-serif">Work Experience</h2>
    <ul class="mt-4 text-2xl leading-9">
      {% for job in work %}
        <li class="flex">
          <div class="flex-grow-0 text-gray-400">{{ job.year }} </div>
          <div>
            <div class="col-span-7">
              {%- if job.url -%}
                <a href="{{ job.url }}" target="_blank" class="inline-flex items-center mr-2"><strong class="underline">{{ job.name }}</strong><span class="text-2xs ml-1 px-1 rounded bg-gray-400 text-black uppercase">View</span></a>
              {%- else -%}
                <strong>{{ job.name }}</strong>
              {%- endif -%}
            </div>
            <div class="col-start-2 col-end-8 font-serif text-gray-400">{{ job.work }}, for <span class="text-almost-white">{{ job.client }}</span></div>
          </div>
        </li>
      {% endfor %}
    </ul>
  
  </div>
{% endblock %}
